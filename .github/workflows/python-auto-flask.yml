name: Python Web Apps Flask Running

on: push

jobs:
  my-job:
    name: Using Flask
    runs-on: ubuntu-latest
    steps:
      - name: Running Tango App
        run: cd src && ./auto.py --run_all n --selected_app 1
