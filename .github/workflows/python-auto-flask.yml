name: Python Web Apps Flask Running

on: push
defaults:
  run:
    shell: bash
    working-directory: ../../src
jobs:
  my-job:
    name: Using Flask
    runs-on: ubuntu-latest
    steps:
      - name: Running Tango App
        run: cd ./auto.py --run_all n --selected_app 1

        
